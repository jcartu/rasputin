{
  "name": "@rasputin/desktop-daemon",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run build.ts",
    "build:current": "bun build --compile ./src/index.ts --outfile dist/rasputin-daemon"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
